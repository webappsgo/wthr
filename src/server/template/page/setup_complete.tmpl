<!DOCTYPE html>
<html lang="en" class="{{if .server.ThemeClass}}{{.server.ThemeClass}}{{else}}theme-dark{{end}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow">
    <title>{{.Title}}</title>
    <link rel="stylesheet" href="/static/css/common.css">
    <link rel="stylesheet" href="/static/css/components.css">
    <link rel="stylesheet" href="/static/css/public.css">
</head>
<body>
    <div class="login-container">
        <div class="login-card">
            <div class="login-header">
                <h1 class="login-title">Setup Complete</h1>
                <p class="login-subtitle">Server Setup - Step 6 of 6</p>
            </div>

            <div class="setup-progress">
                <div class="progress-bar">
                    <div class="progress-fill success" style="width: 100%"></div>
                </div>
                <div class="progress-steps">
                    <span class="step completed">Admin</span>
                    <span class="step completed">API Token</span>
                    <span class="step completed">Server</span>
                    <span class="step completed">Security</span>
                    <span class="step completed">Services</span>
                    <span class="step active">Complete</span>
                </div>
            </div>

            <div class="login-info success-box">
                <p>The Primary Administrator account has been created successfully.</p>
                <p>You can now access the admin panel to configure your server.</p>
            </div>

            <div class="button-group">
                <a href="{{.AdminPath}}/dashboard" class="btn-login">
                    Go to Admin Dashboard
                </a>
                <a href="/" class="btn-secondary">
                    Go to Homepage
                </a>
            </div>
        </div>

        {{ if .version }}
        <div class="login-footer">
            v{{ .version }}
        </div>
        {{ end }}
    </div>
</body>
</html>
