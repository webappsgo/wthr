{{template "head" .}}
{{template "admin_header" .}}
{{- $lang := "en" -}}
{{- if .lang}}{{$lang = .lang}}{{end -}}
{{- $ap := "/server/admin" -}}
{{- if .admin_path}}{{$ap = .admin_path}}{{end -}}
  <div class="admin-header">
    <h1>{{t $lang "admin.cluster_nodes.title"}}</h1>
    <p class="admin-page-intro">{{t $lang "admin.cluster_nodes.intro"}}</p>
  </div>

  <section class="admin-section" aria-labelledby="cluster-nodes-list-heading">
    <div class="section-header">
      <h2 id="cluster-nodes-list-heading">{{t $lang "admin.cluster_nodes.list_heading"}}</h2>
    </div>
    <div class="empty-state">
      <p class="empty-state__icon" aria-hidden="true">&#128421;</p>
      <h3 class="empty-state__title">{{t $lang "admin.cluster_nodes.empty_title"}}</h3>
      <p class="empty-state__message">{{t $lang "admin.cluster_nodes.empty_message"}}</p>
      <a class="btn btn-primary" href="{{$ap}}/config/cluster/add">{{t $lang "admin.cluster_nodes.empty_action"}}</a>
    </div>
  </section>
{{template "admin_footer" .}}
