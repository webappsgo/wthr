# wthr

Read `AI.md` and `IDEA.md` before acting on this project.

`AI.md` (PARTS 0-36 + PART 37 reference) defines HOW to implement things — read-only,
never modify. `IDEA.md` defines WHAT this project does — update as features change.
If `SPEC.md` exists, it overrides AI.md for project-specific rule exceptions.

See `.claude/rules/` for topic-scoped cheatsheets once populated (PART 0 mandates
14 files there — see TODO.AI.md for the pending rule-file generation task).
