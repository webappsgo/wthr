# AI Task Ledger

This file is the repository-local mirror of the active task state so work can move to a new development server without losing context.

## Current In-Progress Task

- [ ] `audit-graphql-runtime` — Continue auditing the live gqlgen runtime against the refreshed `AI.md`. The GraphQL passkey/security parity surface is fully wired (build + vet clean inside `golang:alpine`) and the post-regen sweep of PARTs 11/14/16/17/34 is now done with no drift caused by my recent work. The admin-passkey foundation (server-side schema/model/REST CRUD) is also landed — admin-passkey-login-integration / admin-passkey-graphql-parity / admin-passkey-ui / admin-passkey-audit-log are tracked as separate items below. Next concrete step: pick the highest-priority remaining item below and break it out into its own focused task.

## Pre-Existing Spec Gaps (Partially Closed)

These pre-date the AI.md template sync and the GraphQL passkey work — they did NOT regress. The admin-passkey item is now partially closed (foundation landed); the rest are still open.

- [ ] `restore-ai-config-files` — Commit `73edd1021c0c` ("🔧 Update configuration files") deleted `AI.md`, `CLAUDE.md`, all 14 `.claude/rules/*.md` files, and `.gitignore` from the working tree and the index. **Session 2026-04-28 progress (in progress):** (a) `AI.md` re-copied from `~/Documents/templates/go/AI.md` with placeholders left as-is per PART 0 (PARTS 0-37 are read-only; placeholders stay as `{...}` literals forever — project values live in IDEA.md / CLAUDE.md / `.claude/rules/*` / code, never in AI.md); (b) `.gitignore` regenerated using `gitignore --dir . default` + PART 3 project-specific entries (binaries/, releases/, rootfs/, IDE, AI personal/state files, secrets) — note: PART 3's example .gitignore ignores all of `.claude/`, but PART 1's "AI-Specific Files" table says `.claude/settings.json` + `.claude/rules/*` + `.claude/CLAUDE.md` are TRACKED while only `.claude/projects/`/`cache/`/`state/`/`sessions/`/`*.local.*` are ignored — this regen follows the more-specific PART 1 rule; (c) `CLAUDE.md` recreated from the PART 0 template (~85 lines) with resolved values for `{project_name}=weather`, `{project_org}=apimgr`, etc.; (d) `.claude/rules/ai-rules.md` written (1/14). **Still pending:** 13 more rule files (project-rules, config-rules, binary-rules, backend-rules, api-rules, frontend-rules, features-rules, service-rules, makefile-rules, docker-rules, cicd-rules, testing-rules, optional-rules) per the PART 0 file→PART mapping. After all 14 are written, write `.git/COMMIT_MESS`.
- [ ] `admin-passkey-login-html-form-page` — Admin login passkey integration (`admin-passkey-login-integration` below) now redirects HTML form callers to `${admin_path}/passkey` after password verify when the admin has registered passkeys, but that page does not yet exist. Add an HTML page that reads the `admin_passkey_pending` cookie, calls `/api/v1/auth/admin/passkey/challenge` + `/verify`, runs `navigator.credentials.get()` in between, and on success redirects to the admin dashboard. JSON callers (CLI/scripts) already work end-to-end since they receive the `session_token` directly in the `/auth/login` response and call the challenge/verify endpoints themselves.
- [ ] `admin-passkey-graphql-parity` — GraphQL schema/resolvers for admin passkeys (mirror of the user-side surface added in `wire-graphql-passkey-runtime`). Will require a gqlgen regen following the operational workflow recorded in this file's Verified Progress Notes.
- [ ] `admin-passkey-ui` — `src/server/template/admin/admin_security.tmpl` has zero passkey references today; add a passkey management section parallel to the user-side `src/server/template/page/user/security.tmpl`.
- [ ] `admin-passkey-audit-log` — Add `admin.passkey_added` / `admin.passkey_removed` events to `server_audit_log` (PART 11 line 15565 already mentions `admin.mfa_*` for TOTP/WebAuthn enable-disable; new per-passkey events should follow the same shape).
- [ ] `swagger-annotations-coverage` — PART 14 says "Both Swagger and GraphQL expose identical functionality" and the audit step in PART 0 requires Swagger to match the actual API. Today `src/swagger/annotations.go` is 58 lines with **zero `@Router` annotations**, so the OpenAPI spec served at `/api/{api_version}/openapi.json` is effectively empty. Add `@Router` annotations covering at minimum the routes mounted in `src/main.go` under `/api/v1/auth/*`, `/api/v1/users/*`, and `/api/v1/{admin_path}/*`. This is a much bigger task than the passkey surface alone.
- [ ] `idea-md-passkey-content-source` — Frontend rules (PART 16) say `/server/about` and `/server/help` content MUST come from `IDEA.md` and never from generic placeholders. Verify the help/about pages actually mention the new GraphQL passkey mutations (or at least don't claim a feature surface that's narrower than what's mounted).

## Recently Completed Task

- [x] `admin-passkey-login-integration` — Wire admin passkey into the login flow per AI.md PART 17 line 28679 ("passkey can be used as primary login or as 2FA"). Added `src/server/handler/admin_passkey_login.go` (new) with: an in-memory `adminPendingSessionCache` (15-minute TTL, 30-minute janitor — pending sessions are not persisted because surviving a restart is unnecessary and keeping them off disk avoids leaking pending-state rows in backups); `CreateAdminPendingSession` / `LoadAdminPendingSession` / `DeleteAdminPendingSession`; `AdminHasPasskeys` (used by HandleLogin to decide whether to issue a pending session or complete login outright); a new `passkeyKindAdminLogin` ceremony state that reuses the existing `passkeyCeremonyCache` so all WebAuthn ceremonies (user login, user 2FA, admin login) share one state machine while the kind field + the `adm:` user-handle prefix prevent cross-side confusion; and `BeginAdminPasskeyLoginToken` / `FinishAdminPasskeyLoginToken` cookie-free helpers (verify the WebAuthn credential, mint a full admin session, invalidate the pending state, return the session ID + expiry to the caller). Added two gin handlers on the existing `AdminPasskeyHandler` (`BeginPasskeyChallenge` and `VerifyPasskey`) and mounted them at `/api/v1/auth/admin/passkey/challenge` + `/verify` (public routes — the pending session token / ceremony token authenticate the caller). Modified `src/server/handler/auth.go` `HandleLogin`: after the admin password is verified, check `AdminHasPasskeys`; if yes, issue a pending session token instead of the full admin_session — JSON callers receive `{requires_passkey: true, session_token, challenge_endpoint, verify_endpoint}` and CLI flows can drive the ceremony directly; HTML form callers get a 302 to `${admin_path}/passkey` with the pending token in a short-lived `admin_passkey_pending` cookie. The full project builds inside `golang:alpine` (`go build ./src/...` exit 0) and `go vet ./src/server/handler/ ./src/server/model/` exits 0. **Scope limitation:** the JS-side challenge page at `${admin_path}/passkey` is not yet implemented — tracked as `admin-passkey-login-html-form-page`. JSON/CLI callers already work end-to-end.
- [x] `admin-passkey-foundation` — Land the foundational layer for admin (server-admin) passkey support per AI.md PART 17 line 28674-28683. Added `server_admin_passkeys` table to `src/database/server_schema.go` (mirrors the `user_passkeys` schema in users.db but the admin variant lives in server.db because admins live in server.db per PART 22), added `AdminPasskeyModel` (`src/server/model/admin_passkey.go`) covering List/Count/HasPasskeys/ListCredentials/Create/UpdateCredential/DeleteByID with the same auto-migration pattern (`PRAGMA table_info` + idempotent ALTERs for `transport`/`attestation_type`/`backup_eligible`/`backup_state`) as the user-side, added cookie-free WebAuthn ceremony helpers (`src/server/handler/admin_passkey_helpers.go`: `ListAdminPasskeys`, `DeleteAdminPasskey`, `BeginAdminPasskeyRegistrationToken`, `FinishAdminPasskeyRegistrationToken`) keyed by the same `passkeyCeremonyCache` as the user side but with `models.AdminPasskeyUserHandlePrefix = "adm:"` so the two sides cannot be confused at verify time, and added gin REST routes (`src/server/handler/admin_passkey.go`) for admin passkey CRUD wired into `src/main.go` under `/api/v1/{admin_path}/profile/security/passkeys` (GET/POST/DELETE). The full project builds inside `golang:alpine` (`go build ./src/...` exit 0) and `go vet ./src/server/handler/ ./src/server/model/ ./src/database/` exits 0. **Scope limitation:** this commit is the management surface only — admin login does NOT yet challenge for or accept passkeys as a 2nd factor. That integration, plus GraphQL parity, admin UI, and per-passkey audit-log events, are tracked as separate items above.
- [x] `audit-parts-11-14-16-17-34` — Re-walk PARTs 11/14/16/17/34 of the refreshed AI.md against the project state after the GraphQL passkey wire-up. Verified: (a) the canonical response envelope per PART 14 line 20570-20620 is `{ok, data}` for success and `{ok, error, message, details?}` for errors — the gqlgen-renamed `Ok bool` field on BulkResponse/ContactSubmission/TaskHistory carries `json:"ok"` so JSON output stays canonical; (b) the autobound `GenericResponse{Success bool, json:"ok"}` in `src/graphql/types.go` produces the same `"ok": true` JSON output; (c) PART 11 (line 14279+) calls out passkey private material as "must NEVER appear in any output" — the schema returns only public passkey summaries (id, name, createdAt, lastUsedAt) and uses opaque ceremony tokens for cross-call state, no key material ever leaves the server; (d) PART 11 audit-log table (line 15565) covers `admin.mfa_enabled`/`admin.mfa_disabled` with method `WebAuthn` but does not enumerate per-passkey events for regular users — the existing implementation does not need additional audit logging to match the spec; (e) recovery-key issuance on first passkey registration (PART 17 line 28683 + PART 34 line 52502) is preserved by `FinishPasskeyRegistrationToken`; (f) PART 34 user passkey UI (line 52493-52502 + canonical route table at line 54515-54520) maps to the existing `src/server/template/page/user/security.tmpl` and the canonical `/users/security/passkeys` REST routes — the brief PART 34 prose lines 52497/52508 mention `/users/settings/security` but the canonical route table at line 54520 says `/users/security/passkeys` and the project follows the canonical table; (g) no `panic("not implemented:` stubs remain in `src/graphql/*.go`. Pre-existing gaps surfaced by the sweep are recorded above (admin-passkey-surface, swagger-annotations-coverage, idea-md-passkey-content-source) and are explicitly NOT regressions from the passkey work.
- [x] `wire-graphql-passkey-runtime` — Land the gqlgen regen so the GraphQL passkey surface is fully callable end-to-end. Bumped gqlgen tool to v0.17.83 long enough to write files, then reverted runtime to v0.17.60; added explicit `models:` bindings in `src/graphql/gqlgen.yml` for `APIToken` / `User` / `Notification` / `SavedLocation` / `Setting` so the regen reuses `models.*` shapes instead of generating fresh duplicate types; uncommented `GenericResponse{ Success bool, Message string }` in `src/graphql/types.go` so gqlgen autobinds it; manually re-applied the in-tree fix to `prelude.resolvers.go` (`inputValueResolverImpl`/`typeResolverImpl` wrappers vs. v0.17.60's broken interface receivers); extracted ~900 lines of helpers and per-type resolver methods that gqlgen v0.17.60 wraps in a `/* … */` "preserve" block on every regen into the new `src/graphql/resolvers_helpers.go` so they survive future regens; renamed `Success` → `Ok` for `BulkResponse` / `ContactSubmission` / `TaskHistory` literals where gqlgen now generates the field as `Ok` (only `GenericResponse` still uses the autobind shim); replaced the regen-generated panicking stubs for `genericResponseResolver.Ok` and `notificationResolver.Type` with real implementations. Net result: `src/graphql/generated.go` and `src/graphql/models_gen.go` now contain the dispatch entries for `currentUserPasskeys`, `beginUserPasskeyRegistration`, `finishUserPasskeyRegistration`, `deleteUserPasskey`, `beginUserPasskeyChallenge`, and `finishUserPasskeyChallenge`, the project builds inside `golang:alpine` (`go build ./src/...` exit 0), and `go vet ./src/server/handler/ ./src/graphql/` exits 0.
- [x] `graphql-passkey-surface` — Land the GraphQL passkey/security parity surface that mirrors `/api/v1/users/security/passkeys` and the password-login passkey-as-second-factor flow. Added `UserPasskey`, `PasskeyRegistrationOptions`, `PasskeyChallengeOptions`, and `PasskeyRegistrationResult` types plus the matching query (`currentUserPasskeys`) and mutations (`beginUserPasskeyRegistration` / `finishUserPasskeyRegistration` / `deleteUserPasskey` / `beginUserPasskeyChallenge` / `finishUserPasskeyChallenge`) in `src/graphql/schema.graphqls`, hand-wrote the matching Go types in `src/graphql/types.go`, implemented the resolver methods in `src/graphql/schema.resolvers.go` plus context helpers in the new `src/graphql/passkey_impl.go`, extracted cookie-free WebAuthn ceremony helpers (`BeginPasskeyRegistrationToken` / `FinishPasskeyRegistrationToken` / `BeginPasskeyChallengeToken` / `FinishPasskeyChallengeToken` / `ListUserPasskeys` / `DeleteUserPasskey`) into the new `src/server/handler/passkey_helpers.go` so REST and GraphQL share the same WebAuthn runtime via opaque ceremony tokens, and added `src/graphql/gqlgen.yml`.
- [x] `refresh-ai-md-template` — Re-sync `AI.md` from `~/Documents/templates/go/AI.md` (~+2200 lines, 4201 insertions / 2139 deletions vs. previous), replace `{project_name}`/`{PROJECT_NAME}`/`{project_org}`/`{PROJECT_ORG}`/`{project_repo}`/`{official_site}` placeholders with `weather`/`WEATHER`/`apimgr`/`APIMGR`/`https://github.com/apimgr/weather`/`https://wthr.top`, restore the previously deleted `CLAUDE.md` and `.claude/rules/*.md` files from `HEAD`, and re-anchor NEVER/MUST/COMMIT_MESS rules into the per-project memory store.

## Completed Tasks

- [x] `sync-local-plan-todo` — Create `PLAN.AI.md` in the repository and merge the current tracked work from the session/checkpoints/SQL ledger into repository-local planning files for migration handoff.
- [x] `audit-build-test` — Verify Makefile, test scripts, temp-dir usage, and container-only execution against `AI.md` PART 26/29 and fix drift.
- [x] `audit-graphql-surface` — Align docs, templates, and route metadata with the mounted `/graphql` endpoint while the larger gqlgen runtime cutover was underway.
- [x] `audit-infra` — Verify Docker, compose, workflows, MkDocs, and ReadTheDocs config against `AI.md` PART 27/28/30 and fix drift.
- [x] `audit-multiuser-auth` — Audit PART 34 auth flows for email verification, token prefixes, and strict admin/user separation.
- [x] `audit-multiuser-locations` — Fix active saved-location paths to use the PART 34 `users.db` schema, including models, dashboard counts, and scheduled notification joins.
- [x] `audit-multiuser-routes` — Audit PART 34 route and template surfaces for `/users`, registration redirects, and admin/user separation.
- [x] `audit-multiuser-tests-docs` — Audit PART 34 docs and tests for multi-user coverage and route expectations.
- [x] `audit-routes-handlers` — Verify routes, content negotiation, auth boundaries, and endpoint behavior against `AI.md` PART 13/14/16/17/34 and fix violations.
- [x] `audit-structure-docs` — Compare project structure, README/docs/IDEA/rule files against `AI.md` PART 3/29/30/31 and fix drift.

## Verified Progress Notes

- Continue `audit-graphql-runtime` from the next verified schema/runtime mismatch. `adminTestChannel` now accepts an optional `recipient`, `adminSMTPProviders` now reads shared SMTP presets, and `submitContactForm` now stores the same request metadata/table shape as the live contact fallback path instead of writing blank fields.
- Active admin API stubs in `src/main.go` are now partially replaced with runtime-backed behavior: `/api/v1/admin/profile*` uses the real server-admin model/token/preferences runtime, `/api/v1/admin/server/admins*` now enforces server-admin privacy while using the live admin/invite tables instead of empty or dummy JSON, and `/api/v1/admin/server/status` plus `/api/v1/admin/server/health` now serve live health data instead of hardcoded placeholders.
- The public health surface is now aligned with the verified PART 13 runtime contract: `/healthz` now serves browser HTML, CLI plain text, and explicit JSON from one canonical public-safe payload; `/api/v1/healthz` now always returns that same indented JSON shape; and the old detailed system-health page no longer exposes internal-ish disk/memory/path details on the public route.
- The server-admin invite flow is now aligned with the public auth invite contract: generated links now point at `/auth/invite/server/{token}`, `/api/v1/auth/invite/server/{token}` now supports validate + complete, the public invite page now validates live tokens and lets the invitee set username/password without re-entering email, invite expiry now uses the allowed `1h|6h|24h|48h|7d` values with a `24h` default instead of a hardcoded 15-minute window, and invite acceptance no longer double-hashes admin passwords.
- The multi-user user-invite surface is now aligned with the implemented invite-only model: `/auth/invite/user/{token}` is now a live HTML accept flow, `/api/v1/auth/invite/user/{token}` validates and completes against the same runtime using stored invite email/role instead of a second registration path, `/api/v1/admin/server/users/invites*` now exists for create/list/detail/revoke, the admin UI now has a working `/admin/server/users/invites` page and correct sidebar link, direct admin user creation/password-setting routes are no longer mounted, and `user_invites` now uses a compatible runtime/schema shape with default `invite_expiration_days: 7`.
- GraphQL is now aligned with the invite-only multi-user model: forbidden `adminCreateUser` and `adminUpdateUserPassword` mutations are removed, `adminUsers` and updated user payloads now mask emails, `/graphql` now exposes live `adminUserInvites` list/detail plus `adminCreateUserInvite`/`adminDeleteUserInvite`, and the remaining admin user update/delete mutations now route through the shared user runtime instead of raw SQL writes.
- GraphQL scheduler mutations are now aligned with the live admin scheduler runtime: `/graphql` now exposes `adminUpdateTask(name, enabled)` alongside enable/disable/trigger, and `adminTaskHistory` now accepts an optional `limit` argument instead of hardcoding 50 runs.
- GraphQL server-admin management is now aligned with the live privacy-safe REST runtime: `/graphql` now exposes `adminServerAdmins` summary data, self-only `adminServerAdmin(id)` detail, and live `adminInviteServerAdmin` plus enable/disable/delete mutations with the same self/primary-admin/last-super-admin protections as `/api/v1/admin/server/admins*`.
- GraphQL current-user token management is now aligned with the live multi-user runtime: `/graphql` now exposes `userTokens`, `createUserToken`, and `revokeUserToken`, mirroring the existing `/api/v1/users/tokens` list/create/delete behavior including the 5-token limit, `usr_` token prefix handling, and create-only plaintext token return.
- GraphQL current-user settings are now aligned with the live multi-user runtime: `/graphql` now exposes `userSettings` plus `updateUserSettings(...)`, mirroring the existing `/api/v1/users/settings` account/privacy/notifications/appearance payload and reusing the shared settings runtime instead of a GraphQL-only partial profile path; the shared settings update validation now returns explicit errors for invalid bio length and invalid themes instead of silently accepting bad input.
- GraphQL public multi-user profiles are now aligned with the live runtime: `/graphql` now exposes `publicUserProfile(username)` with the same username/display-name/avatar/bio/location/website/verified/created-at payload as `/api/v1/public/users/:username`, and it reuses the shared public-profile runtime so private profiles still resolve as not found unless the requesting user is viewing their own account.
- GraphQL current-user avatar management is now aligned with the live multi-user runtime: `/graphql` now exposes `currentUserAvatar`, `updateUserAvatar(type, url)`, and `resetUserAvatar`, mirroring the existing `/api/v1/users/avatar` read/update/reset behavior and reusing the shared avatar runtime so HTTPS URL validation and gravatar reset behavior stay consistent across REST and GraphQL.
- GraphQL current-user password changes are now aligned with the live multi-user runtime: `/graphql` now exposes `changeUserPassword(currentPassword, newPassword)` and reuses the shared password-change runtime behind `/api/v1/users/security/password`, preserving the same Argon2id verification/update flow and success message instead of leaving password changes REST-only.
- GraphQL current-user 2FA and recovery-key flows are now aligned with the live multi-user runtime: `/graphql` now exposes `currentUserTwoFactorStatus`, `currentUserTwoFactorSetup`, `enableUserTwoFactor`, `disableUserTwoFactor`, `verifyUserTwoFactor`, and `regenerateUserRecoveryKeys`, all reusing the same shared TOTP/recovery runtime behind `/api/v1/users/security/2fa*` and `/api/v1/users/security/recovery/regenerate`.
- GraphQL current-user avatar uploads are now aligned with the live multi-user runtime: `/graphql` now exposes `uploadUserAvatar(file: Upload!)`, reuses the same shared avatar-upload validation/runtime behind `POST /api/v1/users/avatar`, and caps GraphQL multipart uploads at the same 2 MB limit as the REST avatar upload contract.
- Current-user profile runtime is now aligned across REST and GraphQL: the shared profile update path now correctly writes `display_name` instead of corrupting `username`, `GET /api/v1/users` now reloads the same persisted current-user profile fields, GraphQL `currentUser` now reads from the shared user model, and `updateUserProfile` now returns the same generic success payload as the live `PATCH /api/v1/users` contract instead of a GraphQL-only `User` object.
- The mounted multi-user auth runtime is now closer to the AI.md contract: `/api/v1/auth/login` now returns a temporary `session_token` for pending 2FA instead of leaking `user_id`, `/api/v1/auth/2fa` and `/api/v1/auth/recovery/use` now complete against that pending session token, recovery-key login now reports `remaining_keys`, and `/api/v1/auth/logout` plus `/api/v1/auth/refresh` now invalidate/rotate the real middleware-backed user session instead of reading a nonexistent `session_id` context key.
- GraphQL mounted core auth is now aligned with the live multi-user runtime: `/graphql` now exposes register/login, session-token 2FA completion, recovery-key login, logout, refresh, password-reset request/reset, and email verification mutations, all reusing the shared `/api/v1/auth/*` runtime so the corrected `session_token` challenge flow, session-backed logout/refresh rules, and password/email side effects stay consistent across REST and GraphQL.
- GraphQL invite auth is now aligned with the mounted multi-user runtime: `/graphql` now exposes user-invite and server-admin-invite validation/completion surfaces, all reusing the live `/api/v1/auth/invite/*` runtime so masked-email validation, auto-login-on-user-invite, and server-admin completion behavior stay consistent across REST and GraphQL.
- The mounted multi-user passkey runtime is now aligned much more closely with AI.md: `/auth/passkey` now runs a live browser WebAuthn sign-in flow, `/api/v1/auth/passkey/challenge` and `/api/v1/auth/passkey/verify` are mounted with real WebAuthn ceremony/session handling, `/api/v1/users/security/passkeys` list/register/delete is mounted behind the authenticated user security surface, password login now treats registered passkeys as a second-factor requirement, and the user security page now manages passkeys instead of leaving the feature as placeholder-only UI plus an unused table.
- Continue `audit-graphql-runtime` from the next verified schema/runtime mismatch; the next mounted multi-user auth/spec drift is the still-missing GraphQL passkey/security parity surface now that the REST and web passkey runtime is mounted.
- AI.md was refreshed from `~/Documents/templates/go/AI.md` on 2026-04-28 (~+2200 lines vs. the previous in-tree copy). Placeholders were re-resolved (`weather`, `apimgr`, `https://wthr.top`, `https://github.com/apimgr/weather`). `.claude/rules/*.md` and `CLAUDE.md` had been deleted on the working tree and were restored from `HEAD` — they currently match the previous AI.md generation, so any audit step that relies on rule-file wording should re-confirm against the refreshed PARTs (especially PARTs 11/14/16/17/34) before relying on the rule-file summary alone.
- AI.md was re-copied from `~/Documents/templates/go/AI.md` AGAIN on 2026-04-28 (after commit `73edd1021c0c` deleted `AI.md`, `CLAUDE.md`, all 14 `.claude/rules/*.md`, and `.gitignore`). PARTs 0-5 were re-read. **Correction:** AI.md placeholders (`{project_name}`, `{PROJECT_NAME}`, `{project_org}`, `{PROJECT_ORG}`, `{project_repo}`, `{official_site}`, etc.) are NOT to be replaced inside AI.md — they stay as literal `{...}` tokens because PART 0 says PARTS 0-37 are read-only. Project-specific values (`weather`, `apimgr`, `https://wthr.top`, `https://github.com/apimgr/weather`) belong in IDEA.md, CLAUDE.md, `.claude/rules/*.md`, generated configs, and code — never inside AI.md. The downstream rule-file regeneration (CLAUDE.md, `.claude/rules/*.md`, `.gitignore`) is tracked as `restore-ai-config-files` above.
- gqlgen toolchain note (updated 2026-04-28 after the passkey regen): the project pins gqlgen runtime at v0.17.60 in `go.mod` but the v0.17.60 generator has a prelude template bug — running `gqlgen generate` against it regenerates `prelude.resolvers.go` with invalid receiver types. The repo carries a hand-patched `prelude.resolvers.go` (using `inputValueResolverImpl`/`typeResolverImpl` wrappers) and ships `src/graphql/gqlgen.yml` with `skip_validation: true` so future generate runs at least write files. **Operational regen workflow** (used to wire the GraphQL passkey runtime): from `src/graphql/`, run `go run github.com/99designs/gqlgen generate` inside `golang:alpine`; gqlgen will overwrite `generated.go` / `models_gen.go` / `schema.resolvers.go` and emit a broken `prelude.resolvers.go`; restore the hand-fixed prelude from `git`, strip the gqlgen-emitted `/* … */` "preserve" block at the bottom of `schema.resolvers.go` (the helpers and per-type resolver methods live in `resolvers_helpers.go` instead so they survive regens), and replace any panic-stub resolvers gqlgen wrote into `schema.resolvers.go` with the real implementations from earlier history. The model bindings in `gqlgen.yml` (APIToken/User/Notification/SavedLocation/Setting → `models.*`) are required so the regen reuses the project's shared structs instead of generating duplicate types; without them gqlgen drops the custom resolver interfaces and breaks the `sql.Null*` → `*time.Time` / `*string` conversions. v0.17.83/0.17.84 fix the prelude template differently but introduce a breaking `executableSchema.Complexity` signature change, so they're not a drop-in replacement for the runtime — keep the runtime at v0.17.60 until the project is ready to do a coordinated runtime bump and re-resolve the breakage in any custom executableSchema callers.
